{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "about-window",
  "description": "关于窗（about.html）：固定尺寸面板，自绘标题栏仅关闭钮 + 拖拽区，故只取 close/start-dragging 两项；自定义命令（about_*）不受 capabilities 约束，无需在此列出",
  "local": true,
  "windows": ["about"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
